# Sarvam AI — Hindi STT (Saarika v2.5) + Translation (Mayura v1)
SARVAM_API_KEY=

# ElevenLabs — Instant Voice Clone + Multilingual TTS
ELEVENLABS_API_KEY=
ELEVENLABS_VOICE_ID=

# n8n basic auth — set a real password even for local-only use
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=changeme

# n8n REST API key — generate via Settings > API inside the running n8n UI.
# Only needed for build_pipeline.py to deploy/update workflows via the API.
N8N_LOCAL_API_KEY=
